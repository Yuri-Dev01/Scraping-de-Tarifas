# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['main_automation.py'],
    pathex=[],
    binaries=[],
    datas=[('extract_tariffs_v11.py', '.'), ('extract_nts_v2.py', '.'), ('extract_tbg_final.py', '.'), ('process_tariffs_v3.py', '.')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='main_automation',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
